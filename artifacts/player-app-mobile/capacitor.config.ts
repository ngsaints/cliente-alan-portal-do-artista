import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.portaldoartista.listener',
  appName: 'Portal do Artista Player',
  webDir: 'dist'
};

export default config;
